import './loader.scss';

const Loader = () => {
    return (
        <div data-testid="loader" className="loader-container">
            <span className="loader"></span>
        </div>
    );
};

export default Loader;
